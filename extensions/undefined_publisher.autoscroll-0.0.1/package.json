{
	"name": "autoscroll",
	"displayName": "AutoScroll",
	"description": "Gradually scrolls the editor to center the cursor over time.",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.85.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [],
	"main": "./extension.js",
	"contributes": {
		"commands": [
			{
				"command": "autoscroll.enable",
				"title": "AutoScroll: Enable"
			},
			{
				"command": "autoscroll.disable",
				"title": "AutoScroll: Disable"
			},
			{
				"command": "autoscroll.toggle",
				"title": "AutoScroll: Toggle"
			},
			{
				"command": "autoscroll.enableFocus",
				"title": "AutoScroll: Enable Focus Mode"
			},
			{
				"command": "autoscroll.disableFocus",
				"title": "AutoScroll: Disable Focus Mode"
			},
			{
				"command": "autoscroll.toggleFocus",
				"title": "AutoScroll: Toggle Focus Mode"
			}
		],
		"configuration": {
			"title": "AutoScroll",
			"properties": {
				"autoscroll.enabledOnStartup": {
					"type": "boolean",
					"default": false,
					"description": "Start scrolling automatically when VS Code opens."
				},
				"autoscroll.centerOffset": {
					"type": "integer",
					"default": -5,
					"description": "Offset from viewport center. Negative = target line above middle, positive = below."
				}
			}
		}
	},
	"scripts": {
		"package": "echo. > LICENSE && vsce package --no-git-tag-version --allow-missing-repository && del LICENSE"
	},
	"__metadata": {
		"installedTimestamp": 1772638761207,
		"targetPlatform": "undefined",
		"size": 8741
	}
}